# Currículo HTML - Ademilson Albino

Este repositório contém meu currículo moderno em HTML + CSS. Basta abrir `index.html` no navegador ou publicar via GitHub Pages.

## 📦 Estrutura
- `index.html` — currículo pronto
- `foto-perfil.png` — imagem de perfil

## 🚀 Publicar com GitHub Pages
1. Vá em **Settings → Pages**.
2. Em **Source**, selecione a branch `main` e a pasta `/root`.
3. Clique em **Save**. O site ficará disponível em `https://<seuusuario>.github.io/<repo>`.

## 🛠 Comandos Git
```bash
git init
git add .
git commit -m "Currículo HTML"
git branch -M main
git remote add origin https://github.com/<seuusuario>/<repo>.git
git push -u origin main
```

## 🖨 Exportar para PDF
Abra o `index.html` no navegador e use **Ctrl+P** (ou **Cmd+P** no Mac) → **Salvar como PDF**.
